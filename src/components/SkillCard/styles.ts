import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    buttonSkill: {
        width: 250,
        flexDirection: 'row',
        alignItems: 'center',
        flex: 1,
    },

    textSkill: {
        color: '#fff',
        fontSize: 22,
        fontWeight: 'bold',
    },
});
